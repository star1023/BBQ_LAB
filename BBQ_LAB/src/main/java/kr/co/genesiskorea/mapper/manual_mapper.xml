<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="manual">
	<sql id="whereManualList">
		<if test="searchType != null and searchType != '' ">
		<choose>
			<when test="searchType.equals('searchName')">
			AND NAME LIKE CONCAT('%',#{searchValue},'%')
			</when>
      		<when test="searchType.equals('searchMenuCode')">
      		AND MENU_CODE LIKE CONCAT('%',#{searchValue},'%')
      		</when>
      	</choose>
		</if>
		<if test="searchCategory1 != null and searchCategory1 != '' ">
			AND MENU_TYPE1 = #{searchCategory1}
		</if>
		<if test="searchCategory2 != null and searchCategory2 != '' ">
			AND MENU_TYPE2 = #{searchCategory2}
		</if>
		<if test="searchCategory3 != null and searchCategory3 != '' ">
			AND MENU_TYPE3 = #{searchCategory3}
		</if>
		<if test="searchFileTxt != null and searchFileTxt != '' ">
			AND lcm.MENU_IDX IN (
				SELECT 
					distinct DOC_IDX 
				FROM 
					lab_file 
				WHERE 1 = 1 
					AND DOC_TYPE = 'MENU' 
					AND REGEXP_LIKE(FILE_CONTENT, #{searchFileTxt})
			)											
		</if>
		<if test="searchTeam != null and searchTeam != '' ">
			<choose>
				<when test="searchUser != null and searchUser != '' ">
					AND DOC_OWNER = #{searchUser}
				</when>
				<otherwise>
					AND DOC_OWNER IN (SELECT USER_ID FROM lab_user WHERE 1 = 1 AND EMSTAT = '1' AND ORGAID = #{searchTeam})
				</otherwise>
			</choose>	
		</if>
		<if test="uploadStatus != null and uploadStatus != '' ">
			AND REG_YN = #{uploadStatus}
		</if>
	</sql>

	<select id="selectManualCount" parameterType="map" resultType="int">
		WITH MANUAL AS 
		(
		    SELECT
		    	lcm.MANUAL_IDX
		    	, lcm.REG_YN
		    	, lm.* 
		    FROM 
		    	lab_cook_manual lcm
		    	, lab_menu lm 
		    WHERE 1 = 1
		    	AND lcm.MENU_IDX = lm.MENU_IDX
		    	<choose>
					<when test="listType != null and listType == 'my' "> 
					AND DOC_OWNER = #{userId} 
					</when>
				    <when test="listType != null and listType == 'team'">
				    AND DOC_OWNER IN (
				    	SELECT USER_ID from lab_team_manager_view ltmv
						WHERE ltmv.TEAM_MANAGER_ID = #{userId}
				    ) 
				    </when>
				    <when test="listType != null and listType == 'share'">
				    	AND MENU_IDX IN (SELECT DOC_IDX FROM lab_doc_shared_user WHERE 1 = 1 AND USER_ID = #{userId} AND DOC_TYPE = #{docType})
				    </when>
				    <when test="listType != null and listType == 'all'">
				    
				    </when>
					<otherwise> AND DOC_OWNER = #{userId} </otherwise>
				</choose>
				<include refid="manual.whereManualList" />
		)
		SELECT 
			count(man.MANUAL_IDX) AS cnt
		FROM 
			MANUAL man
		WHERE 1 = 1
			AND man.IS_DELETE = 'N'
			AND man.IS_LAST = 'Y'
	</select>
	
	<select id="selectManualList" parameterType="map" resultType="hashmap">
		WITH MANUAL AS 
		(
		    SELECT
		    	lcm.MANUAL_IDX
		    	, lcm.REG_YN
		    	, lm.* 
		    FROM 
		    	lab_cook_manual lcm
		    	, lab_menu lm 
		    WHERE 1 = 1
		    	AND lcm.MENU_IDX = lm.MENU_IDX 
		    	<choose>
					<when test="listType != null and listType == 'my' "> 
					AND DOC_OWNER = #{userId} 
					</when>
				    <when test="listType != null and listType == 'team'">
				    AND DOC_OWNER IN (
				    	SELECT USER_ID from lab_team_manager_view ltmv
						WHERE ltmv.TEAM_MANAGER_ID = #{userId}
				    ) 
				    </when>
				    <when test="listType != null and listType == 'share'">
				    	AND MENU_IDX IN (SELECT DOC_IDX FROM lab_doc_shared_user WHERE 1 = 1 AND USER_ID = #{userId} AND DOC_TYPE = #{docType})
				    </when>
				    <when test="listType != null and listType == 'all'">
				    
				    </when>
					<otherwise> AND DOC_OWNER = #{userId} </otherwise>
				</choose>
				<include refid="manual.whereManualList" />
		)
		SELECT 
			*
			, CASE WHEN man.REG_YN = 'Y' THEN '등록완료'
				ELSE '미등록'
				END as REG_YN_TXT
			, (SELECT CATEGORY_NAME FROM lab_system_category WHERE 1 = 1 AND CATEGORY_IDX = man.MENU_TYPE1) as CATEGORY_NAME1
			, (SELECT CATEGORY_NAME FROM lab_system_category WHERE 1 = 1 AND CATEGORY_IDX = man.MENU_TYPE2) as CATEGORY_NAME2
			, (SELECT CATEGORY_NAME FROM lab_system_category WHERE 1 = 1 AND CATEGORY_IDX = man.MENU_TYPE3) as CATEGORY_NAME3
			, (SELECT COUNT(*) FROM lab_menu WHERE 1 = 1 AND MENU_CODE = man.MENU_CODE AND IS_LAST = 'N' AND VERSION_NO <![CDATA[<]]> man.VERSION_NO AND IS_DELETE = 'N' ) as CHILD_CNT
			, FN_GET_USER_NAME(DOC_OWNER) as DOC_USER_NAME
		FROM 
			MANUAL man
		WHERE 1 = 1
			AND DOC_NO in (
				SELECT tmp.DOC_NO FROM (
					SELECT 
						row_number() over (order by  manaual.MENU_IDX DESC) as rnum
						, manaual.* 
					FROM 
						MANUAL manaual
					WHERE 1 = 1
						AND manaual.IS_DELETE = 'N'
					)tmp
				WHERE rnum between #{startRow} AND #{endRow}
			)
		ORDER BY man.DOC_NO DESC, man.MENU_IDX DESC	
	</select>
	
	<update id="uploadManual" parameterType="map">
		UPDATE
			lab_cook_manual
		SET
			REG_YN = 'Y'
			, REG_DATE = sysdate()
		WHERE 1 = 1 
			AND MANUAL_IDX = #{idx}
				
	</update>
	
	<select id="selectManualFileList" parameterType="map" resultType="hashmap">
		SELECT
			FILE_IDX
			, DOC_IDX
			, DOC_TYPE
			, FILE_TYPE
			, FILE_NAME 
			, ORG_FILE_NAME
			, FILE_PATH
			, DATE_FORMAT(REG_DATE, '%Y-%m-%d') as REG_DATE
			, FILE_CONTENT
		FROM
			lab_file
		WHERE 1 = 1
			AND DOC_IDX = #{idx}
			AND DOC_TYPE = #{docType}		
	</select>
	
	<insert id="insertManual" parameterType="map">
		INSERT
		INTO lab_cook_manual(MANUAL_IDX, MENU_IDX, REG_YN, REG_DATE)
		VALUES(
			FN_NEXTVAL('lab_manual_seq')
			, #{docIdx}
			, 'N'
			, sysdate()
		)	
	</insert>
</mapper>
